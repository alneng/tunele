name: tunele-dev-deps

services:
  redis:
    image: redis:8-alpine
    container_name: tunele-redis-dev
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 10s
      retries: 5
